# CC-Deck Runtime Data Files
# Only exclude runtime-generated data, keep source code files

# Runtime execution data (exclude)
context/active/*.json
context/completed/
checkpoints/*.json
sessions/*.json
logs/*.log

# Temporary files (exclude)
*.tmp
*.temp
*.log

# Keep important directories and source files
!context/schemas/
!context/templates/
!context/.gitkeep
!checkpoints/.gitkeep
!sessions/.gitkeep
!logs/.gitkeep

# Keep all JavaScript source files in runtime/
!runtime/*.js